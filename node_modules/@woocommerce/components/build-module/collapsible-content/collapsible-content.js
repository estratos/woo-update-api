/**
 * External dependencies
 */
import { createElement, useState } from '@wordpress/element';
import { Icon, chevronDown, chevronUp } from '@wordpress/icons';
export const CollapsibleContent = ({ initialCollapsed = true, toggleText, children, ...props }) => {
    const [collapsed, setCollapsed] = useState(initialCollapsed);
    return (createElement("div", { "aria-expanded": collapsed ? 'false' : 'true', className: `woocommerce-collapsible-content` },
        createElement("button", { className: "woocommerce-collapsible-content__toggle", onClick: () => setCollapsed(!collapsed) },
            createElement("div", null,
                createElement("span", null, toggleText),
                createElement(Icon, { icon: collapsed ? chevronDown : chevronUp, size: 16 }))),
        !collapsed && (createElement("div", { ...props, className: "woocommerce-collapsible-content__content" }, children))));
};
