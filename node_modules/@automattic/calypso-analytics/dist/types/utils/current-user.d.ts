export interface SetCurrentUserParams {
    ID: string;
    username: string;
    email: string;
    localeSlug: string;
}
export interface CurrentUserHashedPii {
    ID: string;
    username: string;
    email: string;
}
export interface CurrentUser {
    ID: number;
    username: string;
    email: string;
    hashedPii: CurrentUserHashedPii;
    localeSlug: string;
}
/**
 * Gets current user.
 * @returns Current user.
 */
export declare function getCurrentUser(): CurrentUser;
/**
 * Sets current user, (stored in javascript memory).
 * @param currentUser the user data for the current user
 * @returns Current user.
 */
export declare function setCurrentUser(currentUser: SetCurrentUserParams): CurrentUser | undefined;
//# sourceMappingURL=current-user.d.ts.map