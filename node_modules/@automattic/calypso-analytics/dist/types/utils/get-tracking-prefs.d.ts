export declare const TRACKING_PREFS_COOKIE_V1 = "sensitive_pixel_option";
export declare const TRACKING_PREFS_COOKIE_V2 = "sensitive_pixel_options";
export type TrackingPrefs = {
    ok: boolean;
    buckets: {
        essential: boolean;
        analytics: boolean;
        advertising: boolean;
    };
};
export declare const parseTrackingPrefs: (cookieV2?: string, cookieV1?: string, defaultPrefs?: TrackingPrefs) => TrackingPrefs;
/**
 * Returns consents for every Cookie Jar bucket based on privacy driven approach
 *
 * WARNING: this function is meant to work on the client side. If not called
 *          from the client side then it defaults to allow all
 * @returns Whether we may track the current user
 */
export default function getTrackingPrefs(): TrackingPrefs;
//# sourceMappingURL=get-tracking-prefs.d.ts.map