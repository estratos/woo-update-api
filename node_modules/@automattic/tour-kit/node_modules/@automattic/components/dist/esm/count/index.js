import { jsx as _jsx } from "react/jsx-runtime";
import clsx from 'clsx';
import { localize } from 'i18n-calypso';
import PropTypes from 'prop-types';
import formatNumberCompact from './format-number-compact';
import './style.scss';
export const Count = ({ count, primary = false, compact = false, forwardRef, numberFormat, translate, locale, ...props }) => {
    return (_jsx("span", { ref: forwardRef, className: clsx('count', { 'is-primary': primary }), ...props, children: compact ? formatNumberCompact(count) || numberFormat(count) : numberFormat(count) }));
};
Count.propTypes = {
    count: PropTypes.number.isRequired,
    numberFormat: PropTypes.func,
    primary: PropTypes.bool,
    compact: PropTypes.bool,
};
export default localize(Count);
//# sourceMappingURL=index.js.map