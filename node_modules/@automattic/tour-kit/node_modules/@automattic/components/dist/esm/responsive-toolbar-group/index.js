import { jsx as _jsx } from "react/jsx-runtime";
import { useBreakpoint } from '@automattic/viewport-react';
import clsx from 'clsx';
import DropdownGroup from './dropdown-group';
import SwipeGroup from './swipe-group';
import './style.scss';
const ResponsiveToolbarGroup = ({ children, className = '', hideRatio = 0.99, showRatio = 1, rootMargin = '0px', onClick = () => null, initialActiveIndex = -1, swipeBreakpoint = '<660px', hrefList = [], forceSwipe = false, swipeEnabled = true, }) => {
    const classes = clsx('responsive-toolbar-group', className);
    const isWithinBreakpoint = useBreakpoint(swipeBreakpoint);
    if (forceSwipe || (swipeEnabled && isWithinBreakpoint)) {
        return (_jsx(SwipeGroup, { className: classes, initialActiveIndex: initialActiveIndex, onClick: onClick, hrefList: hrefList, children: children }));
    }
    return (_jsx(DropdownGroup, { className: classes, initialActiveIndex: initialActiveIndex, onClick: onClick, hideRatio: hideRatio, showRatio: showRatio, rootMargin: rootMargin, children: children }));
};
export default ResponsiveToolbarGroup;
//# sourceMappingURL=index.js.map