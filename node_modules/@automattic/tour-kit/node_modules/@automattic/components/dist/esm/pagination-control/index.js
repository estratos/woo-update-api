import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { __, sprintf } from '@wordpress/i18n';
import clsx from 'clsx';
import { times } from 'lodash';
import './style.scss';
const PaginationControl = ({ activePageIndex, numberOfPages, onChange, classNames, children, }) => {
    const classes = clsx('pagination-control', classNames);
    return (_jsxs("ul", { className: classes, "aria-label": __('Pagination control'), children: [times(numberOfPages, (index) => (_jsx("li", { "aria-current": index === activePageIndex ? 'page' : undefined, children: _jsx("button", { className: clsx('pagination-control__page', {
                        'is-current': index === activePageIndex,
                    }), disabled: index === activePageIndex, "aria-label": sprintf(
                    /* translators: 1: current page number 2: total number of pages */
                    __('Page %1$d of %2$d'), index + 1, numberOfPages), onClick: () => onChange(index) }) }, `${numberOfPages}-${index}`))), children && _jsx("li", { className: "pagination-control__last-item", children: children })] }));
};
export default PaginationControl;
//# sourceMappingURL=index.js.map