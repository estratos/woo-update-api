import { jsx as _jsx } from "react/jsx-runtime";
import clsx from 'clsx';
import CLIENT_LIST from './client-list';
import './style.scss';
const ClientLogoList = (props) => {
    const { className, ...otherProps } = props;
    const elementProps = {
        className: clsx('client-logo-list', className),
        ...otherProps,
        role: 'presentation',
        'aria-hidden': 'true',
    };
    return (_jsx("div", { ...elementProps, children: CLIENT_LIST.map(({ slug, name, Logo }) => (_jsx("div", { className: `client-logo-list__item client-logo-list__item--name-${slug}`, title: name, children: _jsx(Logo, {}) }, slug))) }));
};
export default ClientLogoList;
//# sourceMappingURL=index.js.map