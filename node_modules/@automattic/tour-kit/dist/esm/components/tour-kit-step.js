import { jsx as _jsx } from "react/jsx-runtime";
/**
 * External Dependencies
 */
import { useMobileBreakpoint } from '@automattic/viewport-react';
import classnames from 'classnames';
/**
 * Internal Dependencies
 */
import { classParser } from '../utils';
const TourKitStep = ({ config, steps, currentStepIndex, onMinimize, onDismiss, onNextStep, onPreviousStep, setInitialFocusedElement, onGoToStep, }) => {
    const isMobile = useMobileBreakpoint();
    const classes = classnames('tour-kit-step', `is-step-${currentStepIndex}`, classParser(config.steps[currentStepIndex].options?.classNames?.[isMobile ? 'mobile' : 'desktop']));
    return (_jsx("div", { className: classes, children: _jsx(config.renderers.tourStep, { steps: steps, currentStepIndex: currentStepIndex, onDismiss: onDismiss, onNextStep: onNextStep, onPreviousStep: onPreviousStep, onMinimize: onMinimize, setInitialFocusedElement: setInitialFocusedElement, onGoToStep: onGoToStep }) }));
};
export default TourKitStep;
//# sourceMappingURL=tour-kit-step.js.map